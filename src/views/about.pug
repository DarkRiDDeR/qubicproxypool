extends ../_layout/default.pug

block styles

block scripts
  if !starter
    // Plugins and scripts required by this view
    script(src='/vendors/@coreui/utils/js/index.js')
    script(src='/js/main.js')

block breadcrumb
    +breadcrumb(
    [
      { href: '/', label: 'Home'},
      { label: 'About'}
    ]
  )

block view
  .row
    .col-md-12
      .card.mb-4
        .card-body
          h1 Qubic Proxy Pool
          include ../_partials/pool-description.pug
          br
          include ../_partials/about.pug
      